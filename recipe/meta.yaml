{% set name = "optimix" %}
{% set version = "1.2.3" %}
{% set sha256 = "13b3f126ff57a5a0c243f5930067ec415d4d6b1d9ab4883fb35b15a931dcf9bb" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
    - pytest-runner
  run:
    - python
    - numpy
    - scipy
    - ndarray-listener
    - brent-search
    - tqdm

test:
  requires:
    - pytest
  imports:
    - optimix
  commands:
    - python -c "import sys; import optimix; sys.exit(optimix.test())"

about:
  home: https://github.com/glimix/optimix
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Abstract function optimisation'
  doc_url: http://optimix.readthedocs.io/
  dev_url: https://github.com/glimix/optimix

extra:
  recipe-maintainers:
    - Horta
